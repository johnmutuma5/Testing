See the test written inside src/components/tests

The test tests similarity with a generated snapshot. You can see the __snapshots__ directory in the same folder and have a peek at what the test runner will be comparing with what.
